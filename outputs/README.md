# Outputs

Downloadable deliverables the Chief of Staff generates on request land here
— status reports (`status_report_<timestamp>.md`, via `python main.py
--status`), drafted PRDs, briefing snapshots, and other artifacts you ask
for a file copy of. Nothing in here is synced automatically; treat it as a
local drop folder.
